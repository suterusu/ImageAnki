# <ScreenName> UseCases

<!-- このファイルは1画面専用。別画面のユースケースは別ファイルに分離する -->
<!-- 作成前に画面構成の合意を済ませる -->
<!-- 同ディレクトリに penScript.md と viewScript.md を作成する（初期は空） -->
<!-- ローディングを扱うユースケースでは「表示 -> 読み込み/処理 -> 非表示」の順序を必須にする -->
<!-- 「ローディング状態を非表示にする」を書く場合、同一ユースケース内に先行する「ローディング状態を表示する」を必ず書く -->

## ユースケース1: <ユースケース名>
### 基本フロー
1. ユーザーは...画面を表示する（または...を要求する）。
2. システムはローディング状態を表示する。
3. システムは...を読み込む（または実行する）。
4. システムはローディング状態を非表示にする。
5. システムは...を表示する。
6. ユーザーは...
7. システムは...

### 分岐フローA: <分岐条件>（基本フロー3から分岐）
3-A. システムは...を検知する。
4-A. システムはローディング状態を非表示にする。
5-A. システムは...を表示する。
6-A. システムは...する。

### 例外フローB: <例外条件>（基本フロー3から分岐）
3-B. システムは...でエラーを検知する。
4-B. システムはローディング状態を非表示にする。
5-B. システムはエラーアラートを表示する。

## ユースケース2: <ユースケース名>
### 基本フロー
1. ユーザーは...
2. システムは...

## UI要素
- ...
- ...
- ...

## UI状態
- ...
- ...
- ...

## UIイベント
- 基本フロー1「...」: ...
- 基本フロー1「...」: ...

## セルフチェック（記述前に削除してよい）
- 「ローディング状態を非表示にする」手順がある場合、同一ユースケースに先行する「ローディング状態を表示する」手順がある
- 分岐フロー/例外フローの分岐元番号が、実際の読み込み/処理手順と一致している
- すべての手順が1行1処理になっている（`AしてBする` を含まない）

---

# 具体例（省略なし）

以下は `ProblemListScreen` を想定した、実運用でそのまま真似できる記述例。

# ProblemListScreen UseCases

## ユースケース1: 学習履歴一覧を確認する
### 基本フロー
1. ユーザーは成績一覧タブを開く。
2. システムはローディング状態を表示する。
3. システムは学習履歴一覧を読み込む。
4. システムはローディング状態を非表示にする。
5. システムは履歴概要の一覧を表示する。

### 分岐フローA: 履歴が存在しない（基本フロー3から分岐）
3-A. システムは履歴が0件であることを検知する。
4-A. システムはローディング状態を非表示にする。
5-A. システムは空状態メッセージを表示する。

### 例外フローB: 履歴の読み込みに失敗する（基本フロー3から分岐）
3-B. システムは履歴読み込み時にエラーを検知する。
4-B. システムはローディング状態を非表示にする。
5-B. システムはエラーアラートを表示する。

## ユースケース2: 学習履歴の詳細を表示する
### 基本フロー
1. ユーザーは学習履歴一覧から1件を選択する。
2. システムは選択した学習回IDの成績詳細画面へ遷移する。

## UI要素
- 学習履歴リスト
- 学習履歴セル
- ローディングインジケータ
- 空状態メッセージ
- エラーアラート

## UI状態
- 履歴一覧データ
- 選択中学習回ID
- ローディング表示状態
- 空状態表示状態
- エラーアラート表示状態

## UIイベント
- 基本フロー1「成績一覧タブを開く」: 成績一覧タブをタップする
- 基本フロー1「学習履歴一覧から1件を選択する」: 学習履歴セルをタップする
